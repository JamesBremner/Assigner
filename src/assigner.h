#include <string>
#include <fstream>
#include <sstream>
#include <iostream>
#include <vector>
#include <algorithm>

class cSlot;

void readfile(
    const std::string &fname);

void readstring(
    const std::string &sin);

bool unitTest();

std::string specText();

